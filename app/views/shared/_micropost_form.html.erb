<%= form_for(@micropost) do |f| %>
   <%= render 'shared/error_messages', object: @micropost %>
	<div class="feild">
		<%=h f.text_area :content, placeholder: "Compose new micropost..." %>
    </div>
    <%=h f.submit "Post", class: "btn btn-large btn-primary" %>
<% end %>		